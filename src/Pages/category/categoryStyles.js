import { makeStyles } from "@mui/styles";

const style = makeStyles((theme) => ({
    categoryImg: {
        height: "4rem",
        width: "4rem",
        borderRadius: '50%',
        marginRight: "2rem"
    }, 
    
}));

export default style;